# Deny everything by default
*
.*

# Allow the four campaign-creator skill directories
!campaign-creator/
!campaign-creator/**
!emailbison/
!emailbison/**
!email-spam-fixer/
!email-spam-fixer/**
!spintax-creator/
!spintax-creator/**

# Allow repo meta-files
!.gitignore
!README.md

# Within emailbison — block the live sender cache (has real IDs)
emailbison/sender-cache.md
